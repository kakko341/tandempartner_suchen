<ul class="media-list">
  <% messages.each do |message| %>
    <li class="media">
      <div class="media-left">
        <img class="media-object img-rounded" src="<%= gravatar_url(message.user, { size: 70 }) %>" alt="">
      </div>
      <div class="media-body">
        <div>
          <h4><%= message.title %></h4>
          <p><%= message.content %></p>
          <%= render 'favorites/like_button', message: message %>
        </div>
        <div>
          <% if current_user == message.user %>
            <%= link_to '編集', edit_message_path(message), class: 'btn btn-default btn-xs' %>
            <%= link_to '削除', message, method: :delete, data: { confirm: "本当に削除しますか?" }, class: 'btn btn-danger btn-xs' %>
            <span class="text-muted"><%= message.created_at %></span>
          <% end %>
        </div>
      </div>
    </li>
  <% end %>
  <%= paginate messages %>
</ul>
